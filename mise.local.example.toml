# Copy this file to mise.local.toml and replace the placeholder. The local file
# is gitignored because it contains a personal Wokwi CI credential.
[env]
WOKWI_CLI_TOKEN = "replace-with-your-wokwi-ci-token"
